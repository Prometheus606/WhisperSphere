Aktuell ist die API fertig. man kann neue räume erstellen, nachrichten verschicken und diese anzeigen lassen. 
auch räume löschen mitsamt der nachrichten ist möglich. das password des raumes sowie die nachrichten selbst werden verschlüsselt. 
als key für die nachrichten gilt das passwort des jeweiligen raumes

schritte:
- im frontend oder backend sollte die eingegebene password länge und qualität überprüft werden!
- authentication fertigstellen
- routen in auth und room fertigstellen/ anpassen. waren als api gebaut, soll aber websites rendern statt json zurückzugeben